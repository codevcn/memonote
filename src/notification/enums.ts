export enum ENotificationTypes {
    SET_PASSWORD = 'password.set',
    REMOVE_PASSWORD = 'password.remove',
}

export enum EPagination {
    MAX_NOTIFS_PER_PAGE = 7,
}

export enum EEngNotifMessages {
    SET_PASSWORD = 'Password has been changed by user',
    REMOVE_PASSWORD = 'Password has been removed by user',
}

export enum EViNotifMessages {
    SET_PASSWORD = 'Mật khẩu đã được thay đổi bởi người dùng',
    REMOVE_PASSWORD = 'Mật khẩu đã bị xóa bỏ bởi người dùng',
}
